@model IEnumerable<Eburger.Models.order>

@{
    ViewBag.Title = "Checkorder";
}

<div class="container-fluid mt-4">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <h3 class="font-weight-bold text-center text-warning">My <span class="text-primary text-uppercase">Orders</span> <i class="fas fa-shopping-cart text-success"></i></h3>
            <hr />
        </div>
    </div>

    <div class="row mt-3 justify-content-center">
        <div class="col-3">
            <div class="card">
                <div class="card-body">
                    <div class="list-group" id="list-tab" role="tablist">
                        <a class="list-group-item list-group-item-action " id="list-home-list" data-toggle="list" href="#list-home" role="tab" aria-controls="home">Dashboard</a>
                        <a class="list-group-item list-group-item-action active" id="list-profile-list" href="/Carts/PlacedOrders" role="tab" aria-controls="profile">My Orders</a>
                        <a class="list-group-item list-group-item-action" id="list-profile-list" href="#list-profile" role="tab" aria-controls="profile">Favorites</a>
                        <a class="list-group-item list-group-item-action " id="list-messages-list" href="/Carts" role="tab" aria-controls="messages">My Cart</a>
                        <a class="list-group-item list-group-item-action" id="list-settings-list" href="/Manage/ChangePassword" role="tab" aria-controls="settings">Settings</a>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-md-9">
            <div class="card">
                <div class="card-body">
                    <div class="row">
                        @foreach (var item in Model)
                        {
                            <div class="col-3">
                                @*<a href="/carts/Orderview/@Html.DisplayFor(modelItem => item.Key)">*@
                                <a href="/Carts/PlacedOrderView/@Html.DisplayFor(modelItem => item.orderID)" class="text-decoration-none">
                                    <div class="card">
                                        <div class="card-body">
                                            <img src="~/Images/systemIMG/4233cf24e463e818e4fe2f23244e47cc.gif" style="width:100%;" />
                                        </div>
                                        <div class="card-footer">
                                            <p class="text-secondary text-center font-weight-bold" style="font-size:85%;">Order No. @Html.DisplayFor(modelItem => item.orderID)</p>
                                            <p class="text-center text-secondary font-weight-bold" style="font-size:85%;">@item.orderDate.ToString("dd MMM yyyy")</p>
                                        </div>
                                    </div>
                                </a>
                                @*</a>*@
                            </div>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>

</div>

@*<ul>
    @foreach (var item in Model.GroupBy(i=>i.userID))
    {

    <li>
        @Html.DisplayFor(modelItem => item.Key)
        <ul>

        </ul>
    </li>

    }
    </ul>*@
